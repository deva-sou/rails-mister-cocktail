<h2><%= @cocktail.name %></h2>


<%= link_to 'Add a new ingredient', new_cocktail_dose_path(@cocktail)  %>

<ul>
  <% @doses.each do |dose| %>
    <li><%= dose.ingredient.name %> - <%= dose.description %> - <%= link_to "Delete #{dose.ingredient.name}",dose_path(dose),method: :delete%></li>
  <% end %>
</ul>



<%= link_to 'Back', cocktails_path  %>
